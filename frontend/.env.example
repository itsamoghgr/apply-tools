# Copy to .env. The local docker-compose Postgres uses these defaults.
DATABASE_URL="postgresql://apply:apply@localhost:5432/apply_tools?schema=public"

# Origin of the FastAPI backend (used by Next.js API routes that proxy to it).
BACKEND_URL="http://127.0.0.1:8001"
